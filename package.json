{
  "name": "mern-url-shortener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client-app/url-shortner-app && npm install && npm run build",
    "start": "cd server-app && npm install && npm run build && npm start"
  }
}